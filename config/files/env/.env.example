# Copy this file to config/files/env/.env for local overrides.
# Keep config/files/env/.env untracked.

# Launcher selectors
# DATA_ENGINE_PROJECT_ROOT=C:\path\to\Project_Final
DATA_ENGINE_ENVIRONMENT=dev
# APP_ENV=dev

# Runtime settings are never read from dotenv. Use only explicitly
# allowlisted APP_OVERRIDE__ variables in the process environment.
